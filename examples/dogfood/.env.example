# Local dev only — Wrangler reads these via `secrets.required` in wrangler.jsonc.
# Copy to `.env` and adjust. Never commit `.env`.

DATABASE_URL=postgres://user:pass@localhost:5432/duneta
AUTH_SECRET=change-me-dev-auth
CSRF_SECRET=change-me-dev-csrf
