{
  "$schema": "../../node_modules/wrangler/config-schema.json",
  "name": "duneta",
  "main": "worker.ts",
  "compatibility_date": "2026-06-24",
  "compatibility_flags": ["nodejs_compat", "nodejs_compat_populate_process_env"],
  "vars": {
    "NODE_ENV": "production",
    "AUTH_BASE_URL": "https://your-domain.example"
  }
  // When database.enabled, add a real Hyperdrive id (no localConnectionString):
  // npx wrangler hyperdrive create duneta-db --connection-string="postgres://..."
  // "hyperdrive": [
  //   { "binding": "HYPERDRIVE", "id": "<your-hyperdrive-config-id>" }
  // ]
}
